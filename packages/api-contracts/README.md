# API Contracts

Versioned event payloads shared between independently deployed services.
OpenAPI documents and protobuf schemas can be added alongside the Java
contracts when those transports are introduced.
