spring:
  jpa:
    hibernate:
      ddl-auto: none            # ← KHÔNG tự sửa schema trên prod!
    show-sql: false             # ← Không log SQL

logging:
  level:
    com.fashionstore: INFO      # ← Chỉ log INFO trở lên
    org.hibernate.SQL: WARN